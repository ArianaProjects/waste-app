import { StyleSheet } from "react-native";

export default StyleSheet.create({
  default: {
    height: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10,
    flexDirection: "row",
    alignContent: "center",
    display: "flex",
  },
  text: {
    textAlign: "center",
    alignContent: "center",
    justifyContent: "center",
    height: 40,
    display: "flex",
  },
});
