import { StyleSheet } from "react-native";

export default StyleSheet.create({
  default: {
    height: 40,
    width: 40,
    margin: 12,
    borderWidth: 1,
    padding: 10,
    backgroundColor: "red",
  },
});
